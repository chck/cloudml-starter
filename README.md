# cloudml-starter
A lightweight foundation for cloud ml

## Requirements
Python==3.5.*

## Setup
```bash
# Install dependencies
pip install -e '.[dev]'
pip install -e .

# Emit release tag
python setup.py release
```
